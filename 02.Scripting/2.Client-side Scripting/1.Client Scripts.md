## 01.Client-side Scripting
Read about:
- Catalog Client Scripts and Client Scripts
- Catalog UI Policies and UI Policies
- UI Actions
- UI Page
- UI Properties 
- Service Catalog
- Data Policy 

What is a ui type?

What does the application,global field represent?

Explain the meaning of every variable in the function onChange(control, oldValue, newValue, isLoading, isTemplate)?

What is onCellEdit?

What is Isolate script?

What is an RITM, what is REQ?

What are workspace client scripts?

What is g_form, getControl, Showfieldmessage,addDecoration?

Read about UI Macro, try to create one

You have implemented a new field under the sys_user table called "description". Now you want to give a default value for that everytime a new record is inserted to the table, which type of script are you going to use?

Give examples of UI actions from the incident table 'list view'.

What is the difference between a client script and a ui policy?

What is a data policy and when would you use it versus when would you use a UI Policy?
What is ajax?

> Go to your PDI -> Service Portal Home -> Request Something -> Can we Help  you? -> Create incident
> 1. Create a new variable on the form called "Company Name" of type single line text
Show this variable on the form only if the urgency is marked as 1 - high, so only the most urgent incidents will provide a user's company name.
> 2. Whenever the urgency variable changes, he wants the "Please describe your issue below" variable's value cleared.
> 3. Allow the submission of the form only IF the user's first name who is submitting the form is not "Andrew".
>
>> Once you are done, check everything is working properly - this means:
Make sure the company name is visible only if 1 - high is chosen.
Make sure the  issue description clears every time the urgency variable changes.
Impersonate "Andrew Och" and make sure he cannot submit the create incident form.

Read about UI Context
- Give 2 examples of UI Context
- How to Create a new UI Context

**Exercises:**

Create a new UI Context, the UI Context, for instance, 'High Priority Incident for Network Operations'.

Define UI Context that filters according to the following conditions:
-  Condition 1: Priority = 1 (High priority incidents).
-  Condition 2: Assignment Group = Network Operations.

Filter by those conditions

