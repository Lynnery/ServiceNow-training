## 03.Lists
**Read about Lists and Tables in ServiceNow**

1. What is a table and what is a list
2. How do you search for tables which don't appear in the application navigator(not using the URL)?
3. What does the term Sys ID mean?
4. How do you delete a record from a list and how do you delete multiple records from a list?
5. Show 4 possible ways to filter records in a list(outcome is the same, the actions you do are different).
6. Which tables inherit from task
7. How can ServiceNow lists be customized to meet specific user needs?

**Exercises:**
* Filtering: Get all the incidents which are active, where Created By admin whose Caller's name contains "Lu".
* Filtering: Get all the incidents whose Priority is 5 - Planning/3 - Moderate/1 - Critical, Their state is closed, They made their SLA
